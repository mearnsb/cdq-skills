#!/bin/bash
# Usage: ./gateway-search "query" [--agent <id>] [--deliver]
QUERY="$1"
AGENT_ID="${2:-main}"
DELIVER="${3:-true}"

if [ "$DELIVER" = "true" ]; then
    openclaw agent --agent "$AGENT_ID" --message "$QUERY" --deliver
else
    openclaw agent --agent "$AGENT_ID" --message "$QUERY"
fi